# Main web framework — handles routing, data validation
# and auto-generates the Swagger documentation
fastapi==0.111.0

# The server that runs FastAPI. "standard" adds useful extras
# like automatic reloading when you change your code
uvicorn[standard]==0.29.0

# ORM (Object Relational Mapper) — lets you write Python classes
# instead of raw SQL to interact with your database
sqlalchemy==2.0.30

# Async driver that lets SQLAlchemy talk to PostgreSQL
# async = non-blocking, handles more requests simultaneously
asyncpg==0.29.0

# Handles database migrations — when you change a model,
# Alembic updates the database structure without losing data
alembic==1.13.1

# Validates and structures your data (e.g. checks that an email
# is actually an email, that required fields are not empty)
pydantic==2.7.1
pydantic-settings==2.2.1

# Handles JWT token creation and verification for authentication
python-jose[cryptography]==3.3.0

# passlib stays at 1.7.4 — bcrypt is pinned separately at 4.0.1
# because newer bcrypt versions break passlib's hash handler
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Required to parse form data sent over HTTP
python-multipart==0.0.9

# Async HTTP client — used to call Spotify and OpenAI APIs
# from our backend
httpx==0.27.0

# Redis client — used for caching and session management
redis==5.0.4

# Official OpenAI Python SDK for mood analysis
openai==1.30.1

# Reads environment variables from the .env file
python-dotenv==1.0.1

# Synchronous PostgreSQL driver used by Alembic for migrations
# (asyncpg is async-only and can't be used by Alembic directly)
psycopg2-binary==2.9.9

# Groq SDK — free alternative to OpenAI, same API format
# Used for mood analysis via LLaMA3 model
groq==0.8.0

resend 